# Chat realtime benchmark

Pending execution. The checked-in benchmark commands fail closed unless both
`LEGACY_CHAT_BENCHMARK_URL` and `NEST_CHAT_BENCHMARK_URL` are supplied. Raw
samples are written beneath `test/chat-realtime/load/results/`; the report
derives its numerical acceptance budget from the legacy sample rather than a
fabricated constant.
