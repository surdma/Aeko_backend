import type { AuthenticatedPrincipal } from '../../auth/auth.types'; import { ChatAiPort } from '../../providers/ai/chat-ai.port'; import { botCommandSchema } from './bot.contract'; import { BotStore } from './bot-prisma.client';
export class BotService{constructor(private readonly ai:ChatAiPort,private readonly store:BotStore){}async chat(p:AuthenticatedPrincipal,raw:unknown){const i=botCommandSchema.parse(raw);const reply=await this.ai.reply({userId:p.userId,message:i.message,...(i.personality?{personality:i.personality}:{}),...(i.instruction?{instruction:i.instruction}:{})});await this.store.recordUsage({userId:p.userId,operation:'reply',provider:reply.provider,tokens:reply.tokens,responseTimeMs:reply.responseTimeMs});return reply}execute(p:AuthenticatedPrincipal,o:string,i:Readonly<Record<string,unknown>>){return this.store.execute(p.userId,o,i)}}
